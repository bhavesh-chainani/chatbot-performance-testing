# Copy to ".env" in this folder (same directory as README).

# Required — browser session cookie after you log into the chatbot site (see README).
SESSION_COOKIE=

# Optional overrides
# CHATBOT_URL=https://cfoti.org
# TEST_TYPE=load
