# Chatbot API Configuration
chatbot:
  url: "https://your-chatbot-url.com"
  api_endpoints:
    login: "/api/auth/login"
    send: "/api/chat"

# Load Test Configuration (10 concurrent users)
load_test:
  users: 10
  spawn_rate: 2.0
  run_time: "5m"
